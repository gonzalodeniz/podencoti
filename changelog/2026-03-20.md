# Changelog 2026-03-20

## product-manager

Hora: 06:02:31 UTC

- Actualiza `product-manager/product-backlog.md`, `historias-de-usuario.md`, `roadmap.md` y `refinamiento-funcional.md` para reflejar que `PB-003` sigue abierto en estado `no validado`.
- Sincroniza la issue #5 en GitHub con el estado operativo real y deja explicito el defecto bloqueante sobre rango de presupuesto invalido.
- Reordena la prioridad operativa para que `developer-teams` corrija `PB-003` en la misma rama antes de iniciar `PB-004` o `PB-005`.

## agile-coach

Hora: 07:11:10 UTC

- Actualiza la regla global y los `AGENTS.md` de todos los roles para exigir que cualquier nota o comentario en una issue de GitHub comience con la linea `Rol: <nombre-del-rol>`.
- Refuerza la trazabilidad de autoria en comentarios de arranque, handoff, validacion y cierres administrativos para reducir ambiguedad operativa entre equipos.

## developer-teams

Hora: 08:03:08 UTC

- Retoma la issue `#5 PB-003` en la rama `developer-teams/issue-5-pb-003-filtros-catalogo` tras el estado `no validado` dejado por `qa-teams`.
- Corrige la validacion del rango de presupuesto para que `presupuesto_min > presupuesto_max` genere una solicitud explicita de correccion en HTML y una respuesta `400 Bad Request` en `/api/oportunidades`.
- Amplia la cobertura tecnica con pruebas especificas para el rango invalido en `tests/test_app.py` y `tests/test_opportunity_catalog.py`, y verifica la entrega con `PYTHONPATH=src python3 -m unittest discover -s tests -v` y `python3 -m compileall src tests`.

## qa-teams

Hora: 10:01:57 UTC

- Revisa la reentrega de la issue `#5 PB-003` sobre la rama `developer-teams/issue-5-pb-003-filtros-catalogo` verificando que incluye handoff completo, evidencia de revision de codigo y sincronizacion visible con `main`.
- Ejecuta validacion funcional del catalogo HTML y de `/api/oportunidades`, cubriendo filtros combinados, estado vacio con filtros validos, limpieza de filtros y el flujo alternativo de rango de presupuesto invalido.
- Publica en GitHub el resultado final de la revision con `Estado operativo: validado` y sincroniza el cuerpo de la issue `#5` para reflejar el nuevo estado operativo visible.

## doc-teams

Hora: 12:02:29 UTC

- Actualiza la documentacion de `doc-teams/` para reflejar el estado verificable de `main` a fecha `2026-03-20`, corrigiendo fechas de revision y reforzando la separacion por audiencia en manuales y referencias.
- Corrige una inconsistencia interna en `doc-teams/glosario.md`, donde la entrega minima ejecutable seguia describiendose sin catalogo ni ficha de detalle pese a que ambas superficies ya estan presentes en `main`.
- Deja explicita la diferencia entre trabajo validado en ramas tecnicas y comportamiento realmente integrado en `main`, especialmente para `PB-003`, para evitar que el `changelog` se interprete como evidencia de filtros ya disponibles en la rama principal.
