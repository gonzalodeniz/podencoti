# Changelog 2026-03-19

## product-manager

Hora: 08:45:53 UTC

- Actualizados `product-backlog.md`, `historias-de-usuario.md`, `casos-de-uso.md`, `roadmap.md`, `definicion-de-hecho.md` y `refinamiento-funcional.md` para reforzar trazabilidad y accionabilidad del MVP.
- Cerradas como decision funcional la regla para expedientes mixtos TI y el tratamiento minimo de oportunidades anuladas, desiertas, desistidas o modificadas.
- Normalizados los issues #1 a #8 en GitHub para que incluyan el formato obligatorio de backlog, historia, caso de uso, criterios de aceptacion, dependencias y estado operativo.

## developer-teams

Hora: 09:06:07 UTC

- Creada la rama `developer-teams/issue-4-ficha-detalle-licitacion` para implementar `PB-002` a partir de la base validada de `PB-001`.
- Anadida navegacion desde el catalogo a la ficha de detalle, con salida HTML y JSON, tratamiento de campos no informados y visibilidad de la fuente oficial.
- Extendidos los datos versionados y la logica de oportunidades para reflejar el ultimo dato oficial visible cuando existe una rectificacion o modificacion del expediente.
- Ejecutada la verificacion tecnica con `python3 -m unittest discover -s tests -v` antes de publicar la rama remota del issue.

## qa-teams

Hora: 10:02:44 UTC

- Validada funcionalmente la issue `#4` sobre la rama `developer-teams/issue-4-ficha-detalle-licitacion` con resultado final `Estado operativo: validado`.
- Comprobada la navegacion desde el catalogo a las fichas de detalle y la visualizacion de presupuesto, fecha limite, procedimiento, organismo convocante, estado oficial y fuente oficial.
- Verificados los escenarios de rectificacion, modificacion y datos no informados en HTML y JSON, sin detectar defectos bloqueantes para `PB-002`.
- Publicado el comentario de QA en GitHub con pruebas realizadas, resultados observados, observaciones y riesgos para el seguimiento posterior.

## doc-teams

Hora: 12:01:10 UTC

- Revisada la documentacion funcional de `product-manager/`, la documentacion vigente de `doc-teams/` y la implementacion real en `src/`, `data/` y `tests/`.
- Actualizada la referencia documental de `doc-teams/` con fecha `2026-03-19` y creada una matriz de trazabilidad para distinguir capacidades disponibles, backlog pendiente y contradicciones abiertas.
- Dejadas explicitas las divergencias entre el codigo actual de `main`, la metadata de `pyproject.toml` y las entradas de `changelog/2026-03-18.md` y `changelog/2026-03-19.md` que describen catalogo o fichas no observables en esta rama.

## agile-coach

Hora: 14:02:48 UTC

- Revisados los `AGENTS.md` de raiz, `agile-coach`, `product-manager`, `developer-teams`, `qa-teams` y `doc-teams`, junto con la documentacion operativa vigente de `agile-coach/`.
- Detectados dos huecos residuales de coordinacion: ausencia de una plantilla literal para el arranque de issues en desarrollo y falta de un gate explicito de QA para rechazar handoffs incompletos o ramas desalineadas con `main`.
- Actualizados `AGENTS.md`, `developer-teams/AGENTS.md`, `qa-teams/AGENTS.md` y los artefactos de `agile-coach/` para reforzar visibilidad de ramas activas, disciplina de handoff y control previo a la validacion funcional.

## product-manager

Hora: 15:40:30 UTC

- Actualizados `product-backlog.md`, `historias-de-usuario.md`, `roadmap.md` y `refinamiento-funcional.md` para reflejar que `PB-001` y `PB-002` ya quedaron validados por `qa-teams` y que la siguiente prioridad ejecutable pasa a ser `PB-003`.
- Integrada en `main` la rama `developer-teams/issue-4-ficha-detalle-licitacion`, que ya absorbia tambien la entrega validada de `PB-001`, y verificada la integracion con `python3 -m unittest discover -s tests -v`.
- Cerradas administrativamente las issues `#3` y `#4` en GitHub tras la validacion explicita de `qa-teams`, con eliminacion de las ramas tecnicas ya resueltas y trazabilidad operativa hacia la issue `#5`.

## developer-teams

Hora: 15:50:54 UTC

- Priorizada la issue `#5` `PB-003`, creada la rama `developer-teams/issue-5-pb-003-filtros-catalogo` y publicado el comentario de arranque en GitHub con estado operativo `en desarrollo`.
- Implementados filtros funcionales por palabra clave, rango de presupuesto, procedimiento y ubicacion sobre el catalogo HTML y la API JSON, incluyendo visibilidad de filtros activos y limpieza de criterios.
- Anadidas pruebas tecnicas del dominio y de la app WSGI para combinaciones de filtros, exclusion de presupuestos no informados cuando hay rango y estado vacio comprensible sin resultados.
- Ejecutadas las verificaciones `PYTHONPATH=src python3 -m unittest discover -s tests -v` y `python3 -m compileall src tests` antes de publicar la rama remota del issue.

## qa-teams

Hora: 15:56:14 UTC

- Revisada la issue `#5` sobre la rama `developer-teams/issue-5-pb-003-filtros-catalogo` tras comprobar que el handoff minimo de `developer-teams` estaba completo y que la rama partia limpiamente de `main`.
- Ejecutadas pruebas funcionales sobre el catalogo HTML y `/api/oportunidades`, incluyendo filtros combinados, visualizacion de filtros activos, limpieza de criterios y escenario de estado vacio.
- Detectado defecto bloqueante en el tratamiento de rango de presupuesto invalido: la app muestra un estado vacio normal en lugar de solicitar correccion del rango como exige `CU-03`.
- Publicado en GitHub el resultado de validacion con `Estado operativo: no validado` y la indicacion de corregir el flujo en la misma rama antes de nueva revision.

## doc-teams

Hora: 16:03:47 UTC

- Revisada de nuevo la documentacion funcional de `product-manager/`, la implementacion real de `main` y los manuales vigentes de `doc-teams/` para corregir desalineaciones documentales tras la integracion de `PB-001` y `PB-002`.
- Actualizados `doc-teams/README.md`, `manual-usuario.md`, `manual-tecnico.md`, `manual-administracion.md`, `guia-instalacion.md`, `guia-despliegue.md`, `faq.md` y `matriz-trazabilidad.md` para reflejar el catalogo inicial, la ficha de detalle, las rutas verificables reales y las capacidades todavia no implementadas.
- Dejadas explicitas como dependencias abiertas la falta de filtros, alertas y pipeline en `main`, junto con la desalineacion residual de `pyproject.toml` respecto a la superficie observable del producto.

## agile-coach

Hora: 16:09:21 UTC

- Revisados de nuevo los `AGENTS.md` de raiz, `product-manager`, `developer-teams`, `qa-teams` y `agile-coach`, junto con la evidencia operativa reciente de la issue `#5` en GitHub.
- Detectado que el cuerpo de la issue seguia mostrando `Estado operativo: nuevo` aunque el flujo real ya habia pasado por `en desarrollo`, `listo para qa` y `no validado`, generando backlog visible desfasado.
- Actualizados los acuerdos de proceso para exigir sincronizacion del campo `Estado operativo:` en el cuerpo de la issue en cada transicion relevante y para reforzar la reentrega obligatoria de `developer-teams` tras un `no validado`.

## agile-coach

Hora: 17:17:10 UTC

- Revisados los procedimientos de `product-manager`, `developer-teams` y `qa-teams` para detectar la falta de tiempo y trazabilidad dedicados a revision de codigo, refactorizacion y control de deuda tecnica.
- Actualizados `AGENTS.md`, `product-manager/AGENTS.md`, `developer-teams/AGENTS.md`, `qa-teams/AGENTS.md` y `agile-coach/acuerdos-operativos.md` para hacer obligatoria la auto-revision de codigo, el reporte de refactorizacion aplicada, la trazabilidad de deuda tecnica y el gate de QA sobre calidad interna.
- Fijado que `product-manager` debe convertir la deuda tecnica relevante en backlog o issues separadas antes del cierre administrativo para evitar que la deuda se acumule fuera del sistema de priorizacion.
