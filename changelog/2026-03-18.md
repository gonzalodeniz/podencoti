# Changelog 2026-03-18

## developer-teams

- Se tomo la issue `#2` (`PB-006`) en la rama `feat/pb-006-clasificacion-ti-auditable` y se dejo constancia de `estado operativo: en desarrollo`.
- Se implemento una capa auditable de clasificacion TI en Python con reglas versionadas, coincidencias por palabras clave, necesidades explicitas y prefijos CPV, ademas de exclusiones y casos frontera.
- Se expusieron las rutas ` /clasificacion-ti` y `/api/clasificacion-ti` para que `qa-teams` pueda contrastar reglas y ejemplos representativos sin depender todavia del catalogo de `PB-001`.
- Se anadieron pruebas tecnicas para clasificacion, auditoria de ejemplos y rutas HTTP, con verificacion local mediante `PYTHONPATH=src python3 -m unittest discover -s tests -v`.
