# Changelog 2026-03-18

## product-manager

- Se reviso la vision vigente y se confirmo que no requiere cambio, aunque se reforzo la necesidad de comunicar cobertura inicial en lugar de cobertura total.
- Se actualizaron `product-backlog.md`, `casos-de-uso.md`, `historias-de-usuario.md`, `roadmap.md`, `definicion-de-hecho.md` y `refinamiento-funcional.md` para reflejar el estado real del MVP y mejorar la trazabilidad con backlog, historias, casos de uso e issues.
- Se incorporo en los artefactos el estado validado de `PB-007` y se dejo como siguiente item recomendado `PB-006`.
- Se crearon las issues de GitHub `#6`, `#7` y `#8` para `PB-004`, `PB-005` y `PB-008`.
- Se anadio una actualizacion administrativa en la issue `#1` para dejar constancia de que `PB-007` esta validado por `qa-teams` pero sigue abierto mientras se resuelve su cierre final de integracion.

## developer-teams

- Se trabajo sobre la issue `#2` (`PB-006`) en la rama `feat/pb-006-clasificacion-ti-auditable`.
- Se implementaron reglas auditables de clasificacion TI, incluyendo logica especifica, datos de reglas versionados y pruebas tecnicas asociadas.
- Se ajusto `.gitignore` para excluir artefactos locales del entorno de trabajo y evitar ruido en la rama tecnica.
- Se dejo la entrega preparada para revision de `qa-teams`, aunque posteriormente la validacion funcional detecto incidencias pendientes antes de su fusion a `main`.

## qa-teams

- Se reviso la issue `#2` (`PB-006`) sobre la rama `feat/pb-006-clasificacion-ti-auditable` tomando como referencia criterios de aceptacion y documentacion funcional de producto.
- Se ejecutaron pruebas de validacion sobre la logica de clasificacion, la aplicacion WSGI y la verificacion tecnica declarada por desarrollo con `PYTHONPATH=src python3 -m unittest discover -s tests -v`.
- Se constato que las rutas declaradas por desarrollo para QA, `/clasificacion-ti` y `/api/clasificacion-ti`, no estan expuestas en la aplicacion y responden `404 Not Found`.
- Se detecto ademas que la rama no integra limpia sobre `main` por conflictos en `.gitignore` y `changelog/2026-03-18.md`.
- Se documento el resultado en la issue `#2` con estado final `no validado`, indicando defectos bloqueantes, observaciones, riesgos y acciones requeridas para `developer-teams`.
- Tras la nueva entrega de `developer-teams`, se repitio la validacion de `PB-006` sobre la misma rama y se comprobo que ya integra limpia con `main` mediante una rama temporal de QA.
- Se verifico que `/clasificacion-ti` y `/api/clasificacion-ti` responden `200 OK`, exponen reglas auditables de inclusion, exclusion y casos frontera, y permiten revisar ejemplos funcionales alineados con producto.
- Se confirmo que `python3 -m compileall src tests` y `PYTHONPATH=src python3 -m unittest discover -s tests -v` pasan correctamente en la entrega rehecha.
- Se registro en la issue `#2` la validacion funcional final con estado operativo `validado`, sin defectos bloqueantes para el alcance actual de `PB-006`.

## doc-teams

- Se revisaron `AGENTS.md`, `doc-teams/AGENTS.md`, la documentacion funcional de `product-manager/` y la documentacion existente de `doc-teams/` antes de actualizar manuales.
- Se contrastaron los manuales con el estado real de `main` y se detecto una contradiccion relevante: la rama no contiene la implementacion fuente ni las pruebas versionadas que describian algunos documentos previos.
- Se actualizaron `doc-teams/README.md`, `manual-usuario.md`, `manual-tecnico.md`, `manual-administracion.md` y `guia-instalacion.md` para documentar solo hechos reproducibles en `main`.
- Se anadio `doc-teams/faq.md` para dejar explicitas las contradicciones entre vision de producto, historial documental y estado tecnico observable.
- Se verifico que `python3 -m pip install -e .` funciona, que `python3 -m unittest discover -s tests -v` devuelve `NO TESTS RAN` y que `make run` falla por ausencia de `podencoti.app`, dejando esas limitaciones reflejadas en la documentacion.

## agile-coach

- Se revisaron `AGENTS.md`, `agile-coach/AGENTS.md`, los `AGENTS.md` de producto, desarrollo, QA y documentacion, y la evidencia operativa de las issues `#1` y `#2`.
- Se actualizaron `agile-coach/analisis-proceso.md`, `mejoras-propuestas.md`, `acuerdos-operativos.md` y `metricas-flujo.md` para documentar dos fricciones observables: conflictos por `changelog/` en ramas tecnicas y ausencia de un checkpoint explicito de sincronizacion con `main` antes del handoff a QA.
- Se reforzaron `AGENTS.md`, `product-manager/AGENTS.md`, `developer-teams/AGENTS.md` y `qa-teams/AGENTS.md` para exigir handoffs mas estructurados, sincronizacion de ramas con `main`, y contexto obligatorio cuando una issue permanezca abierta tras `estado operativo: validado`.

## agile-coach

Hora: 14:01:47 UTC

- Se reviso de nuevo la evidencia operativa real en GitHub y se confirmo que `PB-006` ya completo bien la revalidacion, pero `PB-007` sigue abierta tras `validado` con contexto administrativo insuficiente.
- Se actualizaron `AGENTS.md`, `product-manager/AGENTS.md`, `developer-teams/AGENTS.md`, `qa-teams/AGENTS.md` y `agile-coach/AGENTS.md` para exigir plantillas literales de handoff y un comentario administrativo estructurado cuando una issue validada siga abierta.
- Se reforzaron `agile-coach/analisis-proceso.md`, `mejoras-propuestas.md` y `acuerdos-operativos.md` con el nuevo foco de mejora sobre cierre post-validacion y homogeneidad de comentarios.
- Se anadio `agile-coach/riesgos-de-coordinacion.md` para dejar visibles los riesgos residuales que conviene vigilar en backlog abierto, handoffs, `changelog/` y activacion de `doc-teams`.

## product-manager

- Se reviso el estado real de las issues remotas y se confirmo que `PB-006` quedo `no validado` por `qa-teams`, por lo que no debe impulsarse `PB-001` hasta corregir esa entrega.
- Se actualizaron `product-backlog.md`, `historias-de-usuario.md`, `roadmap.md`, `refinamiento-funcional.md` y `definicion-de-hecho.md` para reflejar el bloqueo actual, reforzar el handoff a QA y dejar trazada la rama activa `feat/pb-006-clasificacion-ti-auditable`.
- Se cerro funcionalmente que las alertas del MVP registran coincidencias de forma interna y que el pipeline inicial es individual por usuario, dejando fuera por ahora la notificacion saliente y la colaboracion por empresa.
- Se preparo una actualizacion operativa en GitHub para mantener la issue `#2` alineada con backlog, roadmap y siguiente paso esperado.

## doc-teams

- Se ampliaron los artefactos documentales de `doc-teams/` con `guia-despliegue.md` y `glosario.md` para separar mejor despliegue operativo y terminologia transversal.
- Se actualizo `doc-teams/README.md` para incluir esos nuevos documentos y reforzar la contradiccion entre el contenido versionado de `main` y lo que el historial operativo afirma sobre la entrega tecnica.
- Se reforzaron `doc-teams/manual-tecnico.md` y `doc-teams/faq.md` para dejar explicita la inconsistencia entre `changelog/2026-03-18.md` y el estado realmente reproducible de la rama.
- Se mantuvo el criterio documental de no presentar como disponible ninguna capacidad que no pueda verificarse desde los ficheros versionados y los comandos ejecutados en `main`.

## product-manager

Hora: 15:10:55 UTC

- Se reviso de nuevo GitHub y se confirmo que `PB-006` ya fue revalidado por `qa-teams` con estado operativo `validado`, por lo que el bloqueo documental anterior habia quedado desfasado.
- Se actualizo la trazabilidad funcional en `product-manager/` para reflejar el nuevo estado real: `PB-006` y `PB-007` pasan a cierre administrativo y `PB-001` queda como siguiente prioridad ejecutable.
- Se incorporo `CU-08` para separar el caso de uso de revision auditable de la regla TI del caso de uso del catalogo y evitar ambiguedad entre validacion previa y consumo de oportunidades.
- Se dejo preparada la parte administrativa de las issues validadas y la recomendacion operativa para que `developer-teams` tome a continuacion la issue `#3`.

## qa-teams

Hora: 17:00:36 UTC

- Se reviso la issue `#3` (`PB-001`) y su handoff de `developer-teams` sobre la rama `developer-teams/issue-3-catalogo-inicial`, tomando como referencia backlog, historia `HU-01` y caso de uso `CU-01`.
- Se ejecuto validacion funcional local de la aplicacion WSGI en `/`, `/api/oportunidades`, `/cobertura-fuentes`, `/api/fuentes`, `/clasificacion-ti` y `/api/clasificacion-ti`, verificando la continuidad de superficies previas y el nuevo catalogo visible.
- Se confirmo que el catalogo publica 3 oportunidades TI de fuentes `MVP`, muestra los campos minimos comprometidos y deja fuera un registro `No TI` y un `Caso frontera` segun la regla funcional vigente.
- Se ejecuto `PYTHONPATH=src python3 -m unittest discover -s tests -v` como soporte tecnico adicional y se reviso la cobertura del escenario de estado vacio incluida en la entrega.
- Se documento en la issue `#3` el resultado final con campos obligatorios de QA, sin defectos bloqueantes, observaciones sobre cambios colaterales en `AGENTS.md` y `Estado operativo: validado`.
