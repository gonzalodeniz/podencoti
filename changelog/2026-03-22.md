# Changelog 2026-03-22

## product-manager

Hora: 06:03:00 UTC

- Refuerzo de `PB-009` en backlog, historia, caso de uso, roadmap y refinamiento funcional para hacerlo mas ejecutable por `developer-teams`.
- Fijado orden funcional por olas para `BOC`, `BOP Las Palmas` y `BOE`, junto con el minimo de trazabilidad al origen oficial exigible por oportunidad recopilada.
- Actualizada la issue #9 de GitHub para alinear criterios de aceptacion, alcance y fuera de alcance con los artefactos de producto.
- Detectado bloqueo operativo de publicacion: el `push` al remoto falla en este entorno por falta de credenciales de GitHub.

## developer-teams

Hora: 08:05:49 UTC

- Tomada la issue #9 en GitHub, creada la rama tecnica `developer-teams/issue-9-pb-009-fuentes-reales` y actualizado su estado operativo a `en desarrollo`.
- Implementada una nueva superficie verificable para `PB-009` con priorizacion de fuentes reales oficiales por olas y salida JSON asociada para `BOC`, `BOP Las Palmas` y `BOE`.
- Reforzada la trazabilidad minima por oportunidad visible anadiendo fecha de publicacion oficial en catalogo, detalle y API, manteniendo referencia explicita a `PB-007` y `PB-006`.
- Ejecutada la suite tecnica completa con `PYTHONPATH=src python3 -m unittest discover -s tests -v` con resultado satisfactorio.

## agile-coach

Hora: 08:50:42 UTC

- Creado el nuevo rol `quality-auditor` con carpeta propia, `AGENTS.md`, prompt operativo y script `6_rol-quality-auditor.sh` para incorporar auditorias estructuradas de calidad de codigo al flujo del repositorio.
- Actualizadas las reglas globales y los `AGENTS.md` de `product-manager`, `developer-teams`, `qa-teams`, `doc-teams` y `agile-coach` para fijar el handoff del informe, la creacion de issues tecnicas por desarrollo y la priorizacion por producto.
- Ajustado `README.md` para reflejar el nuevo rol y corregir la secuencia operativa de merge tras validacion de `qa-teams`.

## agile-coach

Hora: 08:56:20 UTC

- Creado el nuevo rol `security-auditor` con carpeta propia, `AGENTS.md`, prompt operativo y script `7_rol-security-auditor.sh` para incorporar auditorias de seguridad de codigo al flujo del repositorio.
- Actualizadas las reglas globales y los `AGENTS.md` de `agile-coach`, `product-manager`, `developer-teams`, `qa-teams`, `doc-teams` y `quality-auditor` para fijar su handoff simultaneo a `product-manager` y `developer-teams`, asi como el tratamiento posterior de hallazgos.
- Definido el alcance del auditor de seguridad sobre SAST, secretos, dependencias, autenticacion, autorizacion, datos sensibles, validacion de entradas, hardening, seguimiento de vulnerabilidades y registro obligatorio en `changelog`.

## agile-coach

Hora: 09:01:42 UTC

- Endurecida la politica de ramas para `quality-auditor` y `security-auditor` para que trabajen solo sobre `main`, sin uso de ramas propias en sus auditorias.
- Ajustada la regla global del repositorio para dejar explicito que ambos auditores quedan limitados a `main` bajo la operativa ordinaria del proyecto.
